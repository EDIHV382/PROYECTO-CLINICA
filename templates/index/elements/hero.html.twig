<div class="hero-section">
    <div class="hero-overlay"></div>
    <div class="container">
        <div class="row align-items-center g-5">

            <div class="col-lg-7 text-center text-lg-start">
                <h5 class="hero-subtitle mb-3">ATENCIÓN ESPECIALIZADA</h5>
                <h1 class="hero-title mb-4">
                    Cuidado Integral del <span class="hero-highlight">Pie Diabético</span>
                </h1>
                <p class="hero-text mb-4">
                    Brindamos un enfoque médico de vanguardia para la prevención, diagnóstico y tratamiento, mejorando la calidad de vida y minimizando complicaciones.
                </p>
                <div class="mt-5">
                    <a href="{{ path('app_blog_view') }}" class="btn btn-primary btn-lg hero-cta-button">Conocer Nuestro Blog</a>
                </div>
            </div>

            <div class="col-lg-5 d-none d-lg-flex justify-content-center">
                <img src="{{ asset('images/logo.png') }}" alt="Logo Clínica Pie Diabético" class="img-fluid hero-logo" loading="lazy" />
            </div>

        </div>
    </div>
</div>

<style>
/* Estilos del Hero (SIN animación de carga) */
.hero-section {
    position: relative;
    padding: 6rem 0;
    min-height: 90vh;
    display: flex;
    align-items: center;
    background: url('{{ asset('images/bg.png') }}') center center/cover no-repeat;
    color: #ffffff;
}

.hero-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(90deg, rgba(15, 23, 42, 0.85) 0%, rgba(15, 23, 42, 0.6) 100%);
    z-index: 1;
}

.hero-section .container {
    position: relative;
    z-index: 2;
}

.hero-subtitle {
    color: #93c5fd;
    font-weight: 600;
    letter-spacing: 2px;
    text-transform: uppercase;
}

.hero-title {
    font-size: 3.5rem;
    font-weight: 800;
    line-height: 1.2;
    color: #ffffff !important;
    text-shadow: 0 4px 15px rgba(0, 0, 0, 0.5);
}

.hero-highlight {
    color: #93c5fd;
}

.hero-text {
    font-size: 1.25rem;
    max-width: 600px;
    color: #e2e8f0 !important;
    text-shadow: 0 2px 8px rgba(0, 0, 0, 0.6);
}

.hero-cta-button {
    padding: 0.8rem 2rem;
    font-weight: 600;
    border-radius: 50px;
    transition: all 0.3s ease;
}
.hero-cta-button:hover {
    transform: translateY(-3px);
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
}

.hero-logo {
    max-width: 320px;
    filter: drop-shadow(0 10px 20px rgba(0,0,0,0.5));
    border-radius: 12px;
}

/* Responsividad del Hero */
@media (max-width: 991.98px) {
    .hero-title { font-size: 2.8rem; }
    .hero-text { font-size: 1.1rem; }
}
@media (max-width: 575.98px) {
    .hero-logo { max-width: 100%; }
}
</style>