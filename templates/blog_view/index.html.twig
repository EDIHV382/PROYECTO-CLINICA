{% extends 'base.html.twig' %}

{% block title %}Blog - Cuidado del Pie Diabético{% endblock %}

{% block body %}
<div class="container mt-5 pb-5">
    <header class="text-center mb-5">
 <div class="featured-section-card">
        <div class="row g-0">
            <div class="col-lg-7">
                <img src="{{ asset('images/bg.png') }}" alt="Cuidado del pie diabético" class="featured-image">
            </div>
            <div class="col-lg-5 d-flex flex-column">
                <div class="featured-content">
                    <h2 class="featured-title">Problemas comunes en pacientes con pie diabético</h2>
                    <p class="featured-excerpt">Es crucial estar atento y protegerse contra complicaciones como infecciones y úlceras para mantener una buena calidad de vida.</p>
                    <a href="#" class="btn btn-outline-primary mt-auto">Leer Artículo Completo <i class="mdi mdi-arrow-right"></i></a>
                </div>
                <div class="posts-list-wrapper">
                    <h5 class="posts-list-title">Más Artículos</h5>
                    <ul class="list-unstyled posts-list">
                        <li>
                            <a href="#" class="post-item">
                                <i class="mdi mdi-account-voice post-icon"></i>
                                <div>
                                    <span class="post-title">Cuándo consultar a un especialista</span>
                                    <small class="post-desc">Revisiones periódicas para prevenir complicaciones.</small>
                                </div>
                            </a>
                        </li>
                        <li>
                            <a href="#" class="post-item">
                                <i class="mdi mdi-bandage post-icon"></i>
                                <div>
                                    <span class="post-title">Cómo cuidar las heridas correctamente</span>
                                    <small class="post-desc">Limpieza y curación para evitar infecciones.</small>
                                </div>
                            </a>
                        </li>
                        <li>
                            <a href="#" class="post-item">
                                <i class="mdi mdi-shield-check-outline post-icon"></i>
                                <div>
                                    <span class="post-title">Prevención de úlceras</span>
                                    <small class="post-desc">Medidas para mantener la piel sana y sin lesiones.</small>
                                </div>
                            </a>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
    </header>

    <nav class="category-filters">
        <a href="{{ path('app_blog_view') }}" class="filter-item {{ activeCategory is null ? 'active' : '' }}">Todos</a>
        {% for category in categories %}
            <a href="{{ path('app_blog_view', {'categoria': category.value}) }}" class="filter-item {{ activeCategory == category.value ? 'active' : '' }}">{{ category.label }}</a>
        {% endfor %}
    </nav>

    <div class="row row-cols-1 row-cols-md-2 row-cols-xl-4 g-4 posts-grid">
        {% for post in posts %}
            <div class="col d-flex align-items-stretch">
                <div class="post-card-horizontal read-more-btn" data-post-id="{{ post.id }}">
                    <div class="post-card-image-wrapper">
                        <img src="{{ asset(post.imageFilename ? 'uploads/images/' ~ post.imageFilename : 'images/placeholder.png') }}" class="post-image" alt="{{ post.title }}">
                    </div>
                    <div class="card-body d-flex flex-column">
                        <p class="post-category">{{ post.categoria }}</p>
                        <h5 class="card-title">{{ post.title }}</h5>
                        <p class="card-text post-excerpt flex-grow-1">{{ post.content|striptags|slice(0, 120) ~ '...' }}</p>
                        <div class="post-meta">
                            <span><i class="mdi mdi-calendar-blank-outline"></i> {{ post.createdAt|date('F d, Y') }}</span>
                            <span><i class="mdi mdi-eye-outline"></i> {{ random(100, 999) }}</span>
                        </div>
                    </div>
                </div>
            </div>
        {% else %}
            <div class="col-12">
                <div class="alert alert-light text-center p-5 border">
                    <h4>No hay publicaciones en esta categoría</h4>
                    <p class="text">Intenta seleccionar "Todos" para ver todos nuestros artículos.</p>
                </div>
            </div>
        {% endfor %}
    </div>
</div>

<div class="modal fade" id="postDetailModal" tabindex="-1" aria-labelledby="postDetailModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered modal-lg modal-dialog-scrollable">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="postDetailModalLabel"></h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Cerrar"></button>
            </div>
            <div class="modal-body-content p-0">
                <img src="" alt="" id="modal-post-image" class="modal-post-image-full" style="display: none;">
                <div class="p-4">
                    <p class="modal-post-meta-text" id="modal-post-meta"></p>
                    <div class="modal-post-body-text" id="modal-post-body"></div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}

{% block stylesheets %}
<style>
    .section-heading {
    font-weight: 700;
    color: var(--color-text);
    text-align: center;
    margin-bottom: 2.5rem;
}

/* --- Tarjeta Principal Destacada --- */
.featured-section-card {
    background-color: var(--color-sidebar);
    border: 1px solid var(--color-border);
    border-radius: 1rem;
    overflow: hidden;
    box-shadow: 0 8px 24px var(--shadow-sm);
    margin-bottom: 4rem;
}

.featured-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
}

.featured-content {
    padding: 2rem;
    display: flex;
    flex-direction: column;
    flex-grow: 1; /* Ocupa el espacio disponible */
}

.featured-title {
    font-weight: 700;
    color: var(--color-text);
    line-height: 1.3;
}

.featured-excerpt {
    color: var(--color-text);
    margin-top: 1rem;
}

/* --- Lista de Posts Secundarios --- */
.posts-list-wrapper {
    padding: 1.5rem 2rem;
    border-top: 1px solid var(--color-border);
}
.posts-list-title {
    font-size: 1rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--color-text);
    margin-bottom: 1rem;
}
.post-item {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 0.75rem;
    border-radius: 0.5rem;
    text-decoration: none;
    transition: background-color 0.3s ease;
}
.post-item:hover {
    background-color: rgba(0,0,0,0.03);
}
body.dark-mode .post-item:hover {
    background-color: rgba(255,255,255,0.05);
}
.post-icon {
    font-size: 1.5rem;
    color: var(--color-primary);
}
.post-title {
    display: block;
    font-weight: 600;
    color: var(--color-text);
}
.post-desc {
    color: var(--color-text);
}

/* --- Sección de Categorías --- */
.categories-section {
    padding-top: 2rem;
}
.category-card {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 1rem;
    padding: 1.5rem;
    background-color: var(--color-sidebar);
    border-radius: 1rem;
    border: 1px solid var(--color-border);
    text-align: center;
    text-decoration: none;
    height: 100%;
    transition: transform 0.3s ease, box-shadow 0.3s ease, border-color 0.3s ease;
}
.category-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 12px 24px var(--shadow-md);
    border-color: var(--color-primary);
}
.category-icon {
    width: 50px;
    height: 50px;
    object-fit: contain;
}
.category-name {
    font-weight: 600;
    color: var(--color-text);
}

/* --- Responsividad --- */
@media (max-width: 991.98px) {
    .featured-image {
        max-height: 400px;
        border-bottom-left-radius: 0;
        border-top-right-radius: 1rem;
    }
}
    .page-title { font-size: 3rem; font-weight: 700; }
    .page-subtitle { font-size: 1.2rem; color: #6c757d; }

    .category-filters {
        display: flex;
        justify-content: center;
        flex-wrap: wrap;
        gap: 1.5rem;
        padding-bottom: 1rem;
        margin-bottom: 3rem;
        border-bottom: 1px solid #eee;
    }
    .category-filters .filter-item {
        color: #6c757d;
        font-weight: 500;
        text-decoration: none;
        transition: color 0.2s ease;
    }
    .category-filters .filter-item.active,
    .category-filters .filter-item:hover {
        color: #0d6efd;
    }

    /* --- Estilo de Tarjeta de Publicación --- */
    .post-card-horizontal {
        background-color: var(--color-bg);
        border: 1px solid #f0f0f0;
        border-radius: 5px;
        cursor: pointer;
        transition: box-shadow 0.3s ease, transform 0.3s ease;
        display: flex;
        flex-direction: column;
        height: 100%;
    }
    .post-card-horizontal:hover {
        transform: translateY(-5px);
        box-shadow: 0 10px 30px rgba(0,0,0,0.07);
    }

    /* --- LA MAGIA PARA IMÁGENES CONSISTENTES --- */
    .post-card-image-wrapper {
        width: 100%;
        overflow: hidden;
        aspect-ratio: 16 / 10; /* Proporción panorámica (puedes cambiar a 4/3, 1/1 para cuadrado, etc.) */
        border-top-left-radius: 4px;
        border-top-right-radius: 4px;
    }
    .post-card-horizontal .post-image {
        width: 100%;
        height: 100%;
        object-fit: cover; /* Rellena el espacio sin deformar la imagen */
        transition: transform 0.3s ease;
    }
    .post-card-horizontal:hover .post-image {
        transform: scale(1.05); /* Efecto de zoom sutil al pasar el mouse */
    }
    /* --- FIN DE LA MAGIA --- */

    .post-card-horizontal .card-body {
        padding: 1.5rem;
        display: flex;
        flex-direction: column;
        flex-grow: 1;
    }
    .post-card-horizontal .post-category {
        color: #a271ac;
        font-size: 0.75rem;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 1px;
    }
    .post-card-horizontal .card-title {
        font-size: 1.2rem;
        font-weight: 600;
        color: #212529;
        margin: 0.5rem 0;
        text-decoration: none;
    }
    .post-card-horizontal .post-excerpt {
        color: #6c757d;
        font-size: 0.95rem;
        line-height: 1.6;
    }
    .post-card-horizontal .post-meta {
        display: flex;
        justify-content: space-between;
        font-size: 0.8rem;
        color: #adb5bd;
        border-top: 1px solid #f0f0f0;
        padding-top: 1rem;
        margin-top: 1.5rem;
    }
    .post-card-horizontal .post-meta span {
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }
    .modal-content {
        background-color: #1e293b;
        color: #e2e8f0;
        border: 1px solid #334155;
        border-radius: 1rem;
    }
    .modal-header { border-color: #334155; }
    .modal-title { font-weight: 600; font-size: 1.8rem; color: #fff; }
    .btn-close { filter: invert(1) grayscale(100%) brightness(200%); }
    .modal-post-image-full {
        width: 100%;
        height: 350px;
        object-fit: cover;
        display: block;
    }
    .modal-post-meta-text {
        color: #5eead4;
        font-weight: 600;
        font-size: 0.9rem;
        margin-bottom: 0.5rem;
    }
    .modal-post-body-text {
        font-size: 1.1rem;
        line-height: 1.7;
        color: #cbd5e1;
    }
</style>
{% endblock %}